import React from 'react';

const Footer = () => {
    return (
        <footer className={'text-center py-10 border border-gray-200'}>
            Footer
        </footer>
    );
};

export default Footer;